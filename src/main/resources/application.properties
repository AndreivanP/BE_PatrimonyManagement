logging.level.org.springframework = info
spring.main.allow-bean-definition-overriding: true

# Render injects the port through PORT; default to 8080 for local runs.
server.port=${PORT:8080}

jwt.signing.key.secret=${JWT_SIGNING_KEY_SECRET:mySecret}
jwt.get.token.uri=/authenticate
jwt.refresh.token.uri=/refresh
jwt.http.request.header=Authorization
jwt.token.expiration.in.seconds=${JWT_TOKEN_EXPIRATION_IN_SECONDS:604800}

# Default Mongo connection kept for local dev; override via SPRING_DATA_MONGODB_URI in production.
#spring.data.mongodb.uri=mongodb://Andreivan:asd123@ec2-54-85-159-102.compute-1.amazonaws.com/pat_manag_production
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://Andreivan:asd123@asset-app-prod.w8mhh.mongodb.net/pat_manag_prod?retryWrites=true&w=majority&appName=asset-app-prod}
